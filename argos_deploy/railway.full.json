{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "/argos_deploy/Dockerfile",
    "watchPatterns": [
      "argos_deploy/**"
    ]
  },
  "deploy": {
    "startCommand": "python3 cloud_entry.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ALWAYS"
  }
}
