# ARGOS external communication safety defaults
EXTERNAL_SEND_ENABLED=false
EXTERNAL_DRAFT_ONLY=true
EXTERNAL_REQUIRE_OWNER_APPROVAL=true
# Railway default is set by argos_deploy/docker-entrypoint.sh under ARGOS_STATE_ROOT.
EXTERNAL_ACTION_AUDIT_PATH=persist/logs/external_actions_audit.jsonl
